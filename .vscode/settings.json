{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:setting}/db.sqlite3",
            "name": "urlshort"
        }
    ]
}